Create a branch named bugFix and check it out.
```bash
git branch bugFix
git checkout bugFix
```

Create and checkout the new branch with the name bugFix in one line.
```bash
git checkout -b bugFix
```
