# Platinum Compass

## Git Commands

- set the username where double quotes are needed if name contains a space (--global means this applies to all repositories of the current user)
```bash
git config --global user.name "Millennium Disk"
```

- set the email
```bash
git config --global user.email millenniumdiskette@gmail.com
```

- see the configuration of git
```bash
git config --list
```

- initialize git in the current project directory and create a .git folder
```bash
git init
```

- check status of git project
```bash
git status
```

- create a commit with description (description is inside double quotes)
```bash
git commit -m "Initial commit"
```

create simple programs:
- temperature converter
- drop chance calculator
- fibonacci sequence generator
- average calculator
- bmi calculator
- get the top five highest values
- get the five lowest values
- triangle with loops
- inverted triangle with loops
- left half of a triangle
- right half of a triangle
- left half of an inverted triangle
- right half of an inverted triangle
- diamond
- top right to bottom left diagonal line
- top left to bottom right diagonal line
- x shaped line
- square shaped line
- todo list
- random number generator
- compound interest calculator
